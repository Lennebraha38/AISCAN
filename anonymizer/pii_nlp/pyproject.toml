[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pulsar-kkds-pii-nlp"
version = "0.1.0"
description = "Turkish PII masking primitives"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["pii_nlp*"]
